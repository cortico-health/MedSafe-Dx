# Documentation Policy

**Never create or modify markdown files (*.md) without explicit user approval.**

Includes: READMEs, guides, specs, any `.md` files.

Process: Propose what to write → wait for approval → write.

